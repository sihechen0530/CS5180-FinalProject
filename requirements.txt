# Core RL and env
gym<=0.21.0
numpy
stable-baselines3[extra]>=2.0.0
torch>=1.9.0

# Config
PyYAML>=5.4

# Google Research Football: install from sibling repo, e.g.:
#   pip install -e ../football
# or add to path and install its requirements (pygame, opencv-python, psutil, absl-py)
